# Implementation Constraints

- Use `apps/site/src/assets/jesus-one.png` as the hero image.
- Keep the hero and benefits readable at 390px, 768px, and 1280px.
- Use benefit-led text only on the landing page.
- Avoid generic gradients, floating cards, filler stats, one-note beige, and nested cards.
- Maintain secondary page compatibility with the shared stylesheet.
- Validate with `pnpm --filter @bible-coder/site lint`, `test`, and `build`, plus visual screenshot checks.
